# 移动端优化速查表

## 🚀 核心优化

### GPU加速
```css
transform: translateZ(0);
backface-visibility: hidden;
will-change: transform;
```

### 防止点击延迟 (200ms)
```css
touch-action: manipulation;
```

### 去除选中高亮
```css
-webkit-tap-highlight-color: transparent;
```

### 平滑动画曲线
```css
transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
```

## 📱 触屏 vs 桌面

### 只在桌面端显示悬停效果
```css
@media (hover: hover) {
    a:hover { opacity: 0.8; }
}
```

### 只在移动端显示点击反馈
```css
@media (hover: none) and (pointer: coarse) {
    a:active { transform: scale(0.98); }
}
```

## ⚡ 优化对象

| 元素 | 优化方法 | 移动端反馈 |
|-----|--------|----------|
| 链接 | GPU加速 + 快速过渡 | :active 缩放 |
| 按钮 | 触屏去高亮 + 活跃态 | 点击缩放 |
| 导航 | will-change | 快速响应 |
| 图片 | transform: scale | 活跃态缩放 |
| 卡片 | 平移动画 | 轻微下压感 |

## 🎯 关键指标

- **点击延迟**：0ms (已消除)
- **动画帧率**：60fps
- **过渡时长**：150-200ms
- **可点击区域**：≥44x44px

## 🔧 检查清单

- [ ] 所有可交互元素有 `touch-action: manipulation`
- [ ] 使用 `translateZ(0)` 启用 GPU 加速
- [ ] 移动端和桌面端有差异化样式
- [ ] 动画使用三次方贝塞尔曲线
- [ ] 没有在过渡中改变 `height/width`（用 `scale` 替代）
- [ ] 图片添加了 `loading="lazy"`
- [ ] 设置菜单使用 `requestAnimationFrame`
- [ ] 测试过实际触屏设备

## 💡 常见问题

### Q: 为什么动画在移动端感觉慢？
A: 检查是否使用了 `ease` 而不是 `cubic-bezier(0.4, 0, 0.2, 1)`

### Q: 点击时为什么还有灰色闪烁？
A: 检查是否添加了 `-webkit-tap-highlight-color: transparent`

### Q: 动画不流畅？
A: 检查是否在过渡中改变了 `layout` 属性（改用 `transform`）

### Q: 如何测试性能？
A: Chrome DevTools → Performance → 录制并查看帧率

## 🎨 动画速度参考

- **快速反馈**：100-150ms
- **标准过渡**：200ms  
- **缓慢动画**：300-500ms
- **避免**：>500ms（感觉不响应）

## 📊 性能监控命令

```bash
# 构建网站
hugo --minify

# 查看生成的文件大小
du -sh public/

# 检查 CSS 大小
wc -c public/css/custom.css
wc -c public/css/mobile-optimize.css
```

## 🌐 浏览器支持

| 特性 | iOS Safari | Chrome Android | Firefox |
|------|-----------|----------------|---------|
| transform | ✅ | ✅ | ✅ |
| will-change | ✅ | ✅ | ✅ |
| touch-action | ⚠️ | ✅ | ✅ |
| @media(pointer) | ⚠️ | ✅ | ✅ |

## 📝 提示

1. **测试真实设备**：模拟器无法真实反映性能
2. **考虑网络**：在3G/4G网络上测试
3. **监控帧率**：目标保持 60fps
4. **用户测试**：收集真实用户反馈
5. **持续优化**：定期检查性能指标

---

**快速参考** | **最后更新**：2026-01-15
